<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.spicetrack">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_spice"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_spice_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Spicetrack"
        tools:targetApi="31">
        <activity
            android:name=".home.ui.scan.Dactivity"
            android:exported="false" />
        <activity
            android:name=".home.ui.dashboard.DashboardActivity"
            android:exported="false" />
        <activity
            android:name=".home.ui.scan.Camera"
            android:exported="false" />
        <activity android:name=".home.ui.search.SearchActivity" />
        <activity
            android:name=".home.ui.scan.ScanActivity"
            android:exported="false" />
        <activity
            android:name=".home.ui.detail.DetailActivity"
            android:exported="false" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".home.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>